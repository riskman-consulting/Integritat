# Nixpacks Configuration for Railway
# Alternative to railway.json with more granular control

[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = ["cd server && npm ci --production"]

[phases.build]
cmds = []

[start]
cmd = "cd server && npm start"

[variables]
NODE_ENV = "production"
